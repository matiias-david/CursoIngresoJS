
function Mostrar()
{
    var base;
	var superficie;
	var resultado;
	base = document.getElementById("laBase").value;
	superficie = value = base*base
    base = parseInt(base);
    superficie = parseInt (superficie);
    alert ("la superficie es" + superficie)
}